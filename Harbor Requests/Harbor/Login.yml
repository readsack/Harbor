info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/team
  body:
    type: json
    data: |-
      {
        "team_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​